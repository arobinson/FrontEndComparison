// Re-export column config from shared-types for consistency across frameworks
export { allColumns, getColumnConfig, getColumnDataType } from 'shared-types';
export type { ColumnConfig, ColumnDataType } from 'shared-types';
